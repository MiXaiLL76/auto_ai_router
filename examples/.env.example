# Example environment file for auto_ai_router tests
# Copy this to .env and fill in your values

# Router URL (default: http://localhost:8080)
ROUTER_BASE_URL=http://localhost:8080/v1

# Master key for authentication (must match master_key in config.yaml)
ROUTER_MASTER_KEY=sk-your-master-key-here

# Note: The master_key will be replaced by the router with the actual
# credential API key from the selected provider in config.yaml
