# Example environment file for auto_ai_router tests
# Copy this to .env and fill in your values

# Router URL (default: http://localhost:8080)
ROUTER_BASE_URL=http://localhost:8080/v1

# Note: API key is not needed for the router as it will be replaced
# The router will use the configured credentials from config.yaml
